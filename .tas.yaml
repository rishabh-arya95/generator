---
framework: mocha
preRun:
     command:
        - npm install
preMerge:
  pattern:
    - "./test/*.js"
postMerge:
  pattern:
    - "./test/*.js"
postRun:
     command:
        - node --version
        - sleep 10000
version: 1.0
